export enum RoadmapMode {
  d3 = 'd3',
  grid = 'grid',
}

export enum ViewMode {
  Detail = 'detail',
  Simple = 'simple',
  List = 'list',
}

export enum DateGranularityState {
  Months = 'months',
  Weeks = 'weeks',
  Quarters = 'quarters',
}

export enum IssueStates {
  OPEN = 'open',
  CLOSED = 'closed',
}
