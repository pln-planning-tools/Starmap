export enum ViewMode {
  Detail = 'detail',
  Simple = 'simple',
  List = 'list',
}

export enum TimeUnit {
  Month = 'month',
  Quarter = 'quarter',
  Year = 'year',
}

export enum DateGranularityState {
  Months = 'months',
  Weeks = 'weeks',
  Quarters = 'quarters',
}

export enum IssueStates {
  OPEN = 'open',
  CLOSED = 'closed',
}
